[project]
name = "ipv6-visualizer"
version = "0.1.0"
description = "IPv6 subnet calculator and visualizer web app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0.0",
]
